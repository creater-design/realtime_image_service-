# Local Model Artifacts

This directory is for local inference deployment artifacts such as:

```text
depth_anything_v2_vits_280.onnx
depth_anything_v2_vits_280.engine
```

Large model files are intentionally ignored by Git. Keep download/export steps in
the documentation instead of committing binary weights.
